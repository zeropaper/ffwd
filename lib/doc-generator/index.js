var DocGenerator = module.exports = function(options) {
  options = options || {};
  
  this.initialize(options);
};

DocGenerator.prototype.initialize = function() {
  
};

DocGenerator.prototype.readSpec = function() {
  
};