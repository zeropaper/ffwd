


/**
 * This callback type is called `miscCallback` and is displayed as a global symbol.
 *
 * @callback miscCallback
 * @param {*} error
 * @param {*} data
 */

/**
 * This callback type is called `stringCallback` and is displayed as a global symbol.
 *
 * @callback stringCallback
 * @param {*} error
 * @param {string} data
 */

/**
 * This callback type is called `arrayCallback` and is displayed as a global symbol.
 *
 * @callback arrayCallback
 * @param {*} error
 * @param {array} data
 */

/**
 * This callback type is called `arrayStringCallback` and is displayed as a global symbol.
 *
 * @callback arrayStringCallback
 * @param {*} error
 * @param {string[]} data
 */

/**
 * This callback type is called `thingSchemaCallback` and is displayed as a global symbol.
 *
 * @callback thingSchemaCallback
 * @param {*} error
 * @param {mongoose.Schema} thingSchema
 */

/**
 * This callback type is called `thingCallback` and is displayed as a global symbol.
 *
 * @callback thingCallback
 * @param {*} error
 * @param {thing} data
 */


/**
 * {@link http://mongoosejs.com/docs/api.html|Mongoose}
 * 
 * @exports mongoose
 * @type {Object}
 * 
 */
var mongoose = {};

/**
 * @class mongoose.Model
 * {@link http://mongoosejs.com/docs/api.html#model_Model|A mongoose.Model}
 */
mongoose.Model = function(){};

/**
 * @class mongoose.Schema
 * {@link http://mongoosejs.com/docs/api.html#schema_Schema|A mongoose.Schema}
 */
mongoose.Schema = function(){};

/**
 * An object containing classes of types
 */
mongoose.Schema.Types = {};

